/**
 * FastBee OpenAPI type surface.
 *
 * Regenerate from the running backend with:
 *   npm run openapi
 *
 * Defaults to http://localhost:8080/v3/api-docs and can be overridden with
 * FASTBEE_OPENAPI_URL.
 */
export interface paths {}
export interface components {
  schemas: {};
  responses: {};
  parameters: {};
  requestBodies: {};
  headers: {};
  pathItems: {};
}
export type webhooks = Record<string, never>;
